#!/usr/bin/env bash
set -euo pipefail

# Compatibility helper only. The documented workflow is `make backend`.
PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec make -C "$PROJECT_ROOT" backend
